import PersonalLoanPage from "@/components/PersonalLoanPage";

export const title = "Vehicle loan";

export const metadata = {
  title: title,
};

export default (props) => <PersonalLoanPage title={title} {...props} />;

Whether it’s time to upgrade from the old faithful, or you want a new car, TLC has a payment plan to accommodate most of your lifestyle needs. Many of us don’t have the change to pay for a new car upfront, especially if it’s a sudden expense, and what some institutions are offering might not be as competitive as you’d hoped.

The only thing you’ll need to shop around for now is the car and not the financing. Call TLC today to explore the competitive payment solutions we have so you can start driving around in your new car sooner rather than later!
