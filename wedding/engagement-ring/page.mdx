import PersonalLoanPage from "@/components/PersonalLoanPage";

export const title = "Your Engagement Ring";

export const metadata = {
  title: title,
};

export default (props) => <PersonalLoanPage title={title} {...props} />;

Every relationship is different and unique, and an engagement ring shouldn’t be any different. Your ring should be symbolic of the love you share and the promise of forever. You know your partner better than anyone so worrying about finances shouldn’t be what’s on your mind.

With a wide range of jewellers and payment plans, make sure the ring is as extraordinary as your partner with TLC.
