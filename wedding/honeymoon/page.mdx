import PersonalLoanPage from "@/components/PersonalLoanPage";

export const title = "Dream Honeymoon";

export const metadata = {
  title: title,
};

export default (props) => <PersonalLoanPage title={title} {...props} />;

Celebrations with your new partner shouldn’t end with the wedding, they should be the beginning, just like it’s the start of a new chapter in your lives. We know that you’ve probably spent a bit on the actual wedding, but why not punctuate the occasion with a getaway to bask in each other’s love and newlywed glow?

There are a few things to consider when planning your honeymoon like where to go and how long to go for, but the only limits you need to worry about are time and budget. Good thing is we can help with your budget, though not so much with time.

TLC’s payment plans are here to suit your unique tastes and help you continue the fairy tale with your partner.

Treat yourselves to some TLC and call us today!
